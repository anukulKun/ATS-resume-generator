# Scripts

These are helper commands used by ATS Resume Generator.

Most users should run them through `npm`:

```bash
npm run doctor
npm run scan
npm run pdf
npm run verify
```

Direct script usage also works:

```bash
node scripts/doctor.mjs
```

Keep new automation scripts in this folder instead of the repository root.

